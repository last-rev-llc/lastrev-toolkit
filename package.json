{
  "name": "@lastrev/toolkit",
  "private": true,
  "engines": {
    "yarn": ">=1.22.4"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "lerna run clean",
    "build": "lerna run build",
    "pub": "lerna publish --independent",
    "test": "jest",
    "test:watch": "jest --watch:"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
